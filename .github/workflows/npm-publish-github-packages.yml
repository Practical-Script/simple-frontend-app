name: Node.js CI

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v3
    - name: Deply to Github Container Registry
      run: |
        docker login --username ${{ secrets.GHCR_USERNAME }} --password ${{ secrets.GHCR_TOKEN }} ghcr.io
        docker build . --tag ghcr.io/${{ secrets.GHCR_USERNAME }}/YOUR_PROJECT_NAME:latest
        docker push ghcr.io/${{ secrets.GHCR_USERNAME }}/simple-frontend-app:latest
